import { NameShapesPipe } from './name-shapes.pipe';

describe('NameShapesPipe', () => {
  it('create an instance', () => {
    const pipe = new NameShapesPipe();
    expect(pipe).toBeTruthy();
  });
});
